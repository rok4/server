name: Build and release

on:
  push:
    tags:
      - '**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:

      - uses: actions/checkout@v2
        with:
          submodules: recursive

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt -y install gettext libfcgi-dev libtinyxml-dev zlib1g-dev libcurl4-openssl-dev libproj-dev libssl-dev libturbojpeg0-dev \
            libjpeg-dev libc6-dev librados-dev libjson11-1-dev libboost-log-dev libboost-filesystem-dev libboost-system-dev libopenjp2-7-dev \
            libproj-dev libsqlite3-dev build-essential cmake

      - name: Build debian package
        run: |
          mkdir build && cd build
          cmake -DCMAKE_INSTALL_PREFIX=/usr -DDEB_PACKAGE=1 -DBUILD_VERSION=${{ github.ref_name }} -DOBJECT_ENABLED=1 ..
          make -j2
          make package

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false

      - name: Add debian package to release
        id: upload-release-asset 
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./build/ROK4GENERATION-${{ github.ref_name }}-Linux-64bit.deb
          asset_name: rok4-generation_${{ github.ref_name }}_amd64.deb
          asset_content_type: application/vnd.debian.binary-package
